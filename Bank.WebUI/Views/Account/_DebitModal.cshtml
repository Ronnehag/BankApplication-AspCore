@model Bank.Application.Accounts.Commands.CreateAccountDebit.CreateAccountDebitCommand

<div class="modal fade" id="withdrawal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">New Debit</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-action="Debit" asp-controller="Account" method="post">
                    <div class="form-group">
                        <label asp-for="AccountId">From Account</label>
                        <input type="number" asp-for="AccountId" class="form-control" readonly="@true" />
                    </div>
                    <div class="form-group">
                        <label asp-for="AccountId">Amount</label>
                        <input asp-for="Amount" step="any" class="form-control num-input"/>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-primary">Withdrawal</button>
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>